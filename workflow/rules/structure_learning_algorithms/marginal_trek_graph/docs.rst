Marginal trek graph
===================

This diagnostic baseline tests every marginal pair ``X_i`` and ``X_j`` with
an empty conditioning set and outputs an undirected marginal-dependence graph.
Edges are removed only for accepted marginal independencies.

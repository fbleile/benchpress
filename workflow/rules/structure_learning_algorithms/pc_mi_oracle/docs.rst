PC-MIOracle
===========

Stable PC uses exactly the same named no-trek sidecar as DAGMA-NOTREKS.
Unconditional queries are answered by membership in that sidecar; every
nonempty-conditioning query uses the ordinary sample Fisher-z test.  The
diagnostics JSON separates oracle answers from statistical test counts.
